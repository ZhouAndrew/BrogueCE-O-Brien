name: "O'Brien Mod v0.2.21"
on:
  push:
    branches: [ master ]
    paths:
      - 'apply_obrien_mod.py'
      - 'apply_obrien_mod_v0_2_1.py'
      - 'apply_obrien_mod_v0_2_2.py'
      - 'apply_obrien_mod_v0_2_3.py'
      - 'apply_obrien_mod_v0_2_4.py'
      - 'apply_obrien_mod_v0_2_5.py'
      - 'apply_obrien_mod_v0_2_6.py'
      - 'apply_obrien_mod_v0_2_7.py'
      - 'apply_obrien_mod_v0_2_8.py'
      - 'apply_obrien_mod_v0_2_9.py'
      - 'apply_obrien_mod_v0_2_10.py'
      - 'apply_obrien_mod_v0_2_11.py'
      - 'apply_obrien_mod_v0_2_12.py'
      - 'apply_obrien_mod_v0_2_13.py'
      - 'apply_obrien_mod_v0_2_14.py'
      - 'apply_obrien_mod_v0_2_15.py'
      - 'apply_obrien_mod_v0_2_16.py'
      - 'apply_obrien_mod_v0_2_17.py'
      - 'apply_obrien_mod_v0_2_18.py'
      - 'apply_obrien_mod_v0_2_19.py'
      - 'apply_obrien_mod_v0_2_20.py'
      - 'apply_obrien_mod_v0_2_21.py'
      - '.github/workflows/obrien-mod-v025.yml'
  workflow_dispatch:

jobs:
  linux-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt update -y
          sudo apt install -y libsdl2-dev libsdl2-image-dev
      - name: Apply O'Brien v0.2.21
        run: python3 apply_obrien_mod_v0_2_21.py
      - name: Verify v0.2.21 markers
        run: |
          grep -q 'Full tricorder identification: type, enchantment, runic and curse state' src/brogue/Items.c
          grep -q 'Bashir carries a finite poison staff' src/brogue/RogueMain.c

          grep -q '#define OBRIEN_PRIMARY_STAFF_CAPACITY 20' src/brogue/Rogue.h
          grep -q '#define OBRIEN_PRIMARY_STAFF_ENCHANTMENT 3' src/brogue/Rogue.h
          grep -q '#define OBRIEN_PRIMARY_STAFF_MARKER (-221)' src/brogue/Rogue.h
          grep -q 'short staffChargeCapacity(const item \*theItem);' src/brogue/Rogue.h

          grep -q 'short staffChargeCapacity(const item \*theItem)' src/brogue/Time.c
          test "$(grep -c 'theItem->charges < staffChargeCapacity(theItem)' src/brogue/Time.c)" -eq 2
          grep -q 'return (theItem->kind == STAFF_BLINKING || theItem->kind == STAFF_OBSTRUCTION ? 10000 : 5000) / theItem->enchant1;' src/brogue/Time.c

          grep -q 'theItem->charges, staffChargeCapacity(theItem)' src/brogue/Items.c
          grep -q 'tempItem->charges = staffChargeCapacity(tempItem);' src/brogue/Items.c
          grep -q 'target->charges >= staffChargeCapacity(target)' src/brogue/Items.c
          grep -q 'staffChargeCapacity(target) - target->charges' src/brogue/Items.c
          grep -q 'theItem->originDepth == OBRIEN_PRIMARY_STAFF_MARKER' src/brogue/Items.c

          grep -q 'obrienAddPrimaryCombatStaffToPack(STAFF_FIRE);' src/brogue/RogueMain.c
          grep -q 'obrienAddPrimaryCombatStaffToPack(STAFF_LIGHTNING);' src/brogue/RogueMain.c
          grep -q 'obrienAddPrimaryCombatStaffToPack(STAFF_POISON);' src/brogue/RogueMain.c
          grep -q 'obrienAddMissionItemToPack(STAFF, STAFF_BLINKING, 10);' src/brogue/RogueMain.c
          grep -q 'obrienAddMissionItemToPack(STAFF, STAFF_TUNNELING, 3);' src/brogue/RogueMain.c
          grep -q 'bashir->carriedItem->enchant1 = OBRIEN_PRIMARY_STAFF_ENCHANTMENT;' src/brogue/RogueMain.c
          grep -q 'bashir->carriedItem->charges = OBRIEN_PRIMARY_STAFF_CAPACITY;' src/brogue/RogueMain.c
          grep -q 'bashir->carriedItem->originDepth = OBRIEN_PRIMARY_STAFF_MARKER;' src/brogue/RogueMain.c

          grep -q '#define OBRIEN_POWER_CELL_CAPACITY 20' src/brogue/Rogue.h
          grep -q '#define OBRIEN_POWER_CELL_TRICKLE_TURNS 100' src/brogue/Rogue.h
          grep -q '#define OBRIEN_POWER_CELL_MARKER (-220)' src/brogue/Rogue.h
          ! grep -q 'OBRIEN_RECHARGE_SPELL_MARKER' src/brogue/Rogue.h

          test "$(grep -c 'obrienAddEmergencyPowerCellToPack();' src/brogue/RogueMain.c)" -eq 3
          grep -q 'cell->originDepth = OBRIEN_POWER_CELL_MARKER;' src/brogue/RogueMain.c
          test "$(grep -c 'obrienAddMissionItemToPack(CHARM, CHARM_RECHARGING, 3);' src/brogue/RogueMain.c)" -eq 1
          grep -q 'Starfleet Emergency Power Cell' src/brogue/Items.c
          grep -q 'useObrienEmergencyPowerCell' src/brogue/Items.c
          grep -q 'obrienFillEmergencyPowerCells' src/brogue/Items.c
          grep -q 'obrienTrickleChargeEmergencyPowerCells' src/brogue/Time.c

          grep -q "O'Brien Must Survive v0.2.21" src/brogue/RogueMain.c
          grep -q 'v0.2.12 clear stairhead lane' src/brogue/RogueMain.c
          ! grep -q 'OBRIEN_RECHARGE_KEY' src/brogue/Rogue.h
          ! grep -q 'obrienRechargeCombatStaffs' src/brogue/Items.c
      - name: Compile
        run: make -B -j3 bin/brogue
