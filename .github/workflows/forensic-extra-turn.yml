name: Forensic extra-turn replay
on:
  push:
    branches: [ master ]
    paths:
      - '.github/workflows/forensic-extra-turn.yml'
      - 'forensic_extra_turn_patch.py'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt update -y
          sudo apt install -y libsdl2-dev libsdl2-image-dev
      - name: Reproduce partial dirty upgrade
        run: |
          python3 apply_obrien_mod_v0_2_23.py
          python3 apply_obrien_mod_v0_2_24.py || true
          git diff -- src/brogue/RogueMain.c src/brogue/Time.c src/brogue/Items.c src/brogue/Monsters.c
      - name: Inject forensic replay
        run: python3 forensic_extra_turn_patch.py
      - name: Compile
        run: make -B -j3 bin/brogue
      - name: Package
        run: |
          mkdir -p artifact/bin
          cp bin/brogue artifact/bin/brogue-forensic-extra-turn
          cp -a bin/assets artifact/bin/
          cp brogue artifact/
          cp src/brogue/RogueMain.c artifact/RogueMain-partial.c
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: forensic-partial-v024
          path: artifact
